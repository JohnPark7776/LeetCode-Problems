class Solution:
    def reverse(self, x: int) -> int:
        y = str(abs(x))
        y = int(y[::-1])
        if y < -(2**31) or y > 2**31:
            return 0
        elif x < 0:
            return -y
        return y
